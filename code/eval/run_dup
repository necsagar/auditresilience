#!/bin/bash

if (( $# != 2 )); then
    echo "Usage: $0 <number_of_parallel_tasks> <loop_count>"
    exit
fi

tasks=$1
iter=$2

cd rdwr
make dup

helper() {
   ./dup $1
}

time ( 
    for (( i=0; i<$tasks; i++ )); do
        (helper $iter) &
    done
    wait 
)

